/**
 * Shared shell dimensions used by teacher and student app layouts.
 * Keeps sidebar and header sizes visually identical across roles.
 */

// Stores the fixed top header height in pixels.
export const SHELL_HEADER_HEIGHT = 60;

// Stores the collapsed sidebar width in pixels.
export const SHELL_SIDEBAR_COLLAPSED_WIDTH = 60;

// Stores the expanded sidebar width in pixels.
export const SHELL_SIDEBAR_EXPANDED_WIDTH = 220;
